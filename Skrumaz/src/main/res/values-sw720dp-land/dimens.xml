<resources>
    <!-- Customize dimensions originally defined in res/values/dimens.xml (such as
         screen margins) for sw720dp devices (e.g. 10" tablets) in landscape here. -->
    <dimen name="activity_horizontal_margin">128dp</dimen>

    <item name="artifact_column_count" format="integer" type="dimen">3</item>

    <item name="iteration_column_count" format="integer" type="dimen">3</item>

    <item name="project_column_count" format="integer" type="dimen">3</item>
</resources>
